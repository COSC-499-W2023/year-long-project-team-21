import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 40,
    width: 250,
    borderColor: 'gray',
    borderWidth: 1,
  },

  space: {
    height: 10,
  },
});

export default styles;
