import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  instructiontitle: {
    flexDirection: 'row',
    flex: 1,
    flexWrap: 'wrap',
    padding: 20,
  },
});

export default styles;
